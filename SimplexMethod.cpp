#include <cmath>
#include <vector>

template<typename Matrix>
Matrix composition(Matrix matrix1, Matrix matrix2)
{
    size_t col = matrix2.at(0).size();
    size_t row = matrix.size();
    Matrix matrix = Matrix(row, typeOf())
}